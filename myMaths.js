const sum = (x, y) => { 
    console.log(x + y)
}

const sub = (x, y) => { 
    console.log(x - y)
}

const mul = (x, y) => { 
    console.log(x * y)
}

function div(x, y) {
    console.log(x/y)
}

const PI = 3.14
const e = 2.71

module.exports = {
    sum,
    sub,
    mul,
    div,
    PI,
    e
}